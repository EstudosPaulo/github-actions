name: EstudoCD
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo Hello, world!


  rspec:
    runs-on: ubuntu-latest
    environment: 
      name: dev
      url: 'portal-dev.dominio.com'
    needs: build
    steps:
      - name: Rspec
        run: echo Rspec
